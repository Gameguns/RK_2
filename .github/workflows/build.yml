name: CI and Release

on:
  push:
    branches: [ main ]
    tags:
<<<<<<< HEAD
      - 'v*'               # запуск при пуше тегов вида v1.0.0, v2.1.3
=======
      - 'v*'
>>>>>>> a468e31 (Use Google Test and auto release)
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: sudo apt update && sudo apt install -y cmake build-essential checkinstall
      - name: Build and test
        run: chmod +x build_and_test.sh && ./build_and_test.sh
      - name: Upload deb artifact
        uses: actions/upload-artifact@v4
        with:
          name: memento-package
          path: memento-pattern.deb

  create-release:
    needs: build-and-test
    runs-on: ubuntu-latest
<<<<<<< HEAD
    if: startsWith(github.ref, 'refs/tags/v')   # только для тегов
=======
    if: startsWith(github.ref, 'refs/tags/v')
>>>>>>> a468e31 (Use Google Test and auto release)
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: memento-package
      - name: Create Release
<<<<<<< HEAD
        id: create_release
=======
>>>>>>> a468e31 (Use Google Test and auto release)
        uses: softprops/action-gh-release@v1
        with:
          files: memento-pattern.deb
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
